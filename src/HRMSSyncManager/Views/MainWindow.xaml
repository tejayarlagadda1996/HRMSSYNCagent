<Window x:Class="HRMSSyncManager.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:HRMSSyncManager.Views"
        Title="HRMS Attendance Sync Manager"
        Height="820"
        Width="1024"
        MinHeight="720"
        MinWidth="900"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterScreen"
        SizeToContent="Manual">
    <Window.Resources>
        <DataTemplate x:Key="SetupWizardTemplate">
            <views:SetupWizardView DataContext="{Binding SetupWizard}"/>
        </DataTemplate>
        <DataTemplate x:Key="DashboardTemplate">
            <views:DashboardView DataContext="{Binding Dashboard}"/>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="24">
        <ContentControl Content="{Binding}"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch">
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Setter Property="ContentTemplate" Value="{StaticResource DashboardTemplate}"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ShowSetupWizard}" Value="True">
                            <Setter Property="ContentTemplate" Value="{StaticResource SetupWizardTemplate}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>
    </Grid>
</Window>
